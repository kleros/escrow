{
  "name": "Kleros Escrow V1",
  "image": "node:10",
  "features": {
    "ghcr.io/enricosecondulfo/devcontainer-features/volta:1": {}
  },
  "postCreateCommand": "yarn install",
  "forwardPorts": [3000],
  "remoteUser": "node"
  // Uncomment if running into ARM64 issues
  //"runArgs": ["--platform=linux/amd64"]
}
